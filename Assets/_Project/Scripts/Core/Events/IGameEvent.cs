namespace EcosDelLaberinto.Core.Events
{
    /// <summary>
    /// Marker interface for every message that travels through the <see cref="EventBus"/>.
    /// Events are immutable value objects describing something that already happened.
    /// </summary>
    public interface IGameEvent
    {
    }
}
